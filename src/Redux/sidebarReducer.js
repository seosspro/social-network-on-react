let initialState = [
    { name: "Серега" },
    { name: "Валера" },
    { name: "Жека" },
    { name: "Андрей" },
    { name: "Макс" },
];

const sidebarReducer = (state = initialState, action) => {
    return state;
};

export default sidebarReducer;
